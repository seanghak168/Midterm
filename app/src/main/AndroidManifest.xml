<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ITEOnlineShop"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.EditActivity"/>
        <activity android:name=".ui.activity.CultureActivity"/>
        <activity android:name=".ui.activity.ShowPopularActivity"/>
        <activity android:name=".ui.activity.HistoryActivity"/>
        <activity android:name=".ui.activity.MountainActivity"/>
        <activity android:name=".ui.activity.MuseumsActivity"/>
        <activity android:name=".ui.activity.SeaActivity"/>
        <activity android:name=".ui.activity.TempleActivity"/>
        <activity android:name=".ui.activity.DetailActivity"/>
    </application>
</manifest>